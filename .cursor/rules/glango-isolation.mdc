---
description: Glango is NOT Silent Ghost — repo, runtime, and Cursor rules are separate
alwaysApply: true
---

# Glango ↔ Silent Ghost isolation

**Glango and Silent Ghost are completely different projects.** Do not merge patterns, imports, or rules across them.

## Glango (this repo)

| Item | Value |
|------|--------|
| Path | `c:\Users\userguest\Desktop\new-project` |
| Git | `https://github.com/paulop9504-dotcom/glango.git` |
| Package | `glango` |
| Dev server | `http://localhost:3000` |
| Product | Personal Action OS — chat, orchestrator, share → action |

**Use Glango rules only:** `.cursor/rules/glango-*.mdc`

## Silent Ghost (other repo — do NOT apply here)

| Item | Value |
|------|--------|
| Path | `c:\Users\userguest\Desktop\ghostsilence-programmer` |
| Git | `ghostsilence-programmer` |
| Bridge | `http://127.0.0.1:38471` |
| Product | VS Code extension + Figma bridge + truth log / projections |

**Never in Glango code:**

- `appendCanonicalEvent`, `UnifiedTruthLineageLog`, `ProjectionRegistry`
- `siteToolEgress`, `deriveNarrative` / SITE_VOCAB engine-site patterns
- Silent Ghost `npm test` / `golden:protocol` as Glango verification

## Workspace

Open **`glango.code-workspace`** (this folder) for Glango-only work.
Multi-root: **`Desktop/glango-and-silent-ghost.code-workspace`** — edit the folder that matches the task.

## Legacy storage keys

`blink-local-links` and similar keys are **Glango-internal legacy names** only — not shared with Silent Ghost runtime.
