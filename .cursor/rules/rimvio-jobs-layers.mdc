---
description: Rimvio Jobs Layers — L0~L5 experience, surface, interaction, enrichment, data
globs: app/**,components/**,lib/layers/**,lib/enrichers/**,lib/share/**
alwaysApply: true
---

# Rimvio Jobs Layers 👀

> **「That's it.」— 설명 30초, 행동 1탭.**

## L0 Experience (철학 — 절대 불변)

- **One thing at a time** — Stack(`/stack`)은 맨 위 1장만 선명
- **Share → Now → Done** — inbox 거치지 않음
- **No scroll guilt** — 전체 스크롤은 `/inbox` deep link
- **1–2 Tap Rule** — Primary action은 풀-width 히어로 버튼

## L1 Surface (화면 계층) — **SSOT: `lib/surface-registry/rimvio-surface-ia.ts`**

| Route | Nav | Surface | 역할 |
|-------|-----|---------|------|
| `/` | 지구 | **Globe (home)** | 지구본 · 핀 · recall (Three Floors 1층) |
| `/field` | 맞춤 | **Field** | 맞춤 sheet ingress (bottom nav — no page) |
| `/peers` | 친구 | **People** | 친구 · ROOM 대화 |
| (sheet) | 기록 | **Capture** | ＋ sheet — 사진 · 링크 · 메모 |
| `/search` | — | **Capture hub** | 수집 · composer (`rimvio:search`) |
| `/now` | — | **Now** | Share 직후 Action sheet |
| `/stack` | — | **Stack** (dev) | 다음 행동 1장 + ghost stack |
| `/inbox` | — | **Inbox** | 전체 목록 (secondary) |

**Dev-only (404 in production):** `/stack` · `/demo` · `/metrics` — `requireDevPage()`

**Legacy redirects:** `RIMVIO_REDIRECTS` — `/feed` → `/` · `/chat` → `/search` · `/archive` → `/?filter=archive` · `/globe` → `/`

**금지:** `/stack`에 카드 피드 3개 이상 나열. 홈(`/`)은 Globe-first — Feed 슬롯 UX는 `/feed` 리다이렉트 뒤 recall deep link만.

## L2 Interaction (행동 계층)

1. **Share bridge** (`/share`) — invisible, toast only
2. **Now sheet** (`/now`) — Primary CTA + Done
3. **Stack dismiss** — swipe/Done → dismissed → 다음 장
4. **Prefetch** — Primary action hover/touch shadow

## L3 Enrichment (지능 계층)

`.cursor/rules/rimvio-enrichment.mdc` 참조.
`Generic → Domain → Intent` → `facts[]` + `actions[]`

## L4 Data (저장 계층)

- Supabase `links` (prod) / `localStorage` + legacy key `blink-local-links` (dev fallback)
- `blink-now-link` — Share → Now handoff (legacy key name; Rimvio-only)
- Chat: `sessionStorage` action-chat scopes

## L5 Intelligence

- Orchestrator pipeline + event kernel (deterministic gates first)
- LLM fallback in Phase 3 only
- **입력:** structured facts/actions. raw HTML 금지 in UI

---

## Share flow

```
공유 → /share (invisible)
     → /now?url=… (persist: false, enrich preview)
     → Primary CTA or Done
     → persist: true (background) → / (Globe) or /stack
```

## 파일 맵

```
lib/surface-registry/rimvio-surface-ia.ts
lib/layers/index.ts
lib/local-links/now-session.ts
lib/action-chat/orchestrator/run-orchestrator-pipeline.ts
components/action-stack-list.tsx
components/now-action-focus.tsx
app/now/page.tsx
app/page.tsx              — Globe home (GlobeHomeClient)
app/search/page.tsx       — Capture / search hub
app/peers/page.tsx        — People tab
app/stack/page.tsx
```

## 금지

- ❌ Share 후 `/inbox` 스크롤만 보내기 (Now sheet 필수)
- ❌ spinner
- ❌ Silent Ghost engine patterns (truth log, site egress)
