/** Map pin bias when brain detail dock occupies the lower band. */
export const BRAIN_SURFACE_DOCK_PIN_VIEWPORT_Y = 0.38;

/** CSS length for the dock row (grid) or max card height. */
export const BRAIN_SURFACE_DOCK_ROW_HEIGHT = "min(46vh, 21rem)";

export const BRAIN_SURFACE_DOCK_CARD_MAX_HEIGHT = "min(44vh, 20rem)";
